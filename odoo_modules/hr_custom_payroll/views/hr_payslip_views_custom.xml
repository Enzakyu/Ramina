<?xml version="1.0" encoding="UTF-8"?>
<odoo>
    
    <record id="view_hr_payslip_tree" model="ir.ui.view">
        <field name="name">hr.payslip.tree</field>
        <field name="model">hr.payslip</field>
        <field name="arch" type="xml">
            <list string="Payslips">
                <field name="number"/>
                <field name="employee_id"/>
                <field name="name"/>
                <field name="date_from"/>
                <field name="date_to"/>
                <field name="state"/>
                <field name="company_id" groups="base.group_multi_company"/>
                <field name="net_wage"/>
            </list>
        </field>
    </record>

    <record id="view_hr_payslip_form" model="ir.ui.view">
        <field name="name">hr.payslip.form</field>
        <field name="model">hr.payslip</field>
        <field name="arch" type="xml">
            <form string="Payslip">
            <header>
                <button string="Confirm" name="action_payslip_done" type="object" invisible="state not in ('draft','verify')" class="oe_highlight"/>
                <button string="Set to Draft" name="action_payslip_draft" type="object" invisible="state != 'cancel'"/>
                <button string="Compute Sheet" name="compute_sheet" type="object" invisible="state != 'draft'" class="oe_highlight"/>
                <button string="Cancel Payslip" name="action_payslip_cancel" type="object" invisible="state not in ('draft','hr_check','confirm','verify')"/>
                <field name="state" widget="statusbar" statusbar_visible="draft,confirm"/>
            </header>
            <sheet>
                <div class="oe_title">
                    <label for="employee_id" class="oe_edit_only"/>
                    <h1><field name="employee_id" placeholder="Employee"/></h1>
                </div>
                <group col="4">
                    <label for="date_from" string="Period"/>
                    <div>
                        <field name="date_from" class="oe_inline"/> - <field name="date_to" class="oe_inline"/>
                    </div>
                    <field name="name"/>
                    <field name="number"/>
                    <field name="company_id" groups="base.group_multi_company"/>
                </group>
                <notebook>
                    <page string="Salary Computation">
                        <field name="line_ids" colspan="4" nolabel="1">
                            <list string="Salary Structure" editable="bottom">
                                <field name="name"/>
                                <field name="code"/>
                                <field name="total"/>
                            </list>
                            <form string="Payslip Line">
                                <group>
                                    <field name="name"/>
                                    <field name="code"/>
                                    <field name="total"/>
                                </group>
                            </form>
                        </field>
                    </page>
                    <page string="Overtime Bonuses">
                        <field name="overtime_bonus_ids">
                            <list>
                                <field name="date"/>
                                <field name="worked_hours"/>
                                <field name="overtime_hours"/>
                                <field name="bonus_amount"/>
                                <field name="state"/>
                            </list>
                        </field>
                    </page>
                </notebook>
            </sheet>
            </form>
        </field>
    </record>

    <record id="action_view_hr_payslip_form" model="ir.actions.act_window">
        <field name="name">Employee Payslips</field>
        <field name="res_model">hr.payslip</field>
        <field name="view_mode">list,form</field>
    </record>

</odoo>
